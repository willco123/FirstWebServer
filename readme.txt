This Project is a simple back-end webserver that can:
  Handle CRUD requests using express,
  Allow users to login with varying permissions,
  Store user data and posts with PostgreSQL as the database,
  Post messages to a message board,
